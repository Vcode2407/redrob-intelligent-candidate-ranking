# Data Directory

Place the challenge files here if you want default local discovery:

- `candidates.jsonl` or `candidates.jsonl.gz`
- `validate_submission.py` if you want to pass it to `--official-validator`

Large challenge data is intentionally not committed.

